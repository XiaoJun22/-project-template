# {{PROJECT_NAME}} — Claude 项目协作说明

## 项目背景
> 待填写：本项目的产品类型、核心功能、目标MCU型号

## 目录规范
本项目遵循固定分层结构，新增代码请对号入座，不要新建平级目录：

- `03_software/03_bsp/` — 板级外设初始化 + 中断向量入口（只做寄存器级操作，不含业务逻辑）
- `03_software/04_drv/` — 器件驱动 + 信号调理（滤波、单位换算、校准，输出物理量）
- `03_software/05_app/` — 应用层：main.c、状态机、控制逻辑（恒流/保护/满充判断等），只消费物理量，不碰寄存器
- `03_software/06_utils/` — 通用工具函数，不依赖硬件

详细说明见 `01_docs/项目目录结构说明.md`。

## 代码风格
- 参考 STM32 HAL 库命名风格
- 函数名：`snake_case`，如 `motor_init()`
- 类型名：`PascalCase` + `TypeDef`，如 `Motor_HandleTypeDef`
- 宏/常量：`ALL_CAPS`
- 注释：英文，HAL风格 Doxygen 块注释

## 中断处理原则
- ISR 只做：清中断标志、读取数据到缓冲区、置位标志位
- 复杂逻辑（保护判断、状态切换）放主循环轮询处理，不在中断里执行
- 中断入口统一放 `03_bsp/bsp_it.c`，只做一行转发调用

## 当前阶段
> 待填写：EVT / DVT / PVT，当前正在进行的工作
